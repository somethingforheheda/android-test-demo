<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Test"
    tools:targetApi="31">
    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:label="@string/app_name"
      android:theme="@style/Theme.Test">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <!-- UI测试相关 -->
    <activity android:name=".tests.ui.ShadowTextTestActivity" />
    <activity
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:name=".tests.ui.FragmentContainerActivity" />
    <activity
        android:name=".FragmentListActivity"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
    <activity
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:name=".tests.ui.RxJavaActivity" />
        
    <!-- 架构模式演示 -->
    <activity
        android:name=".mvp.view.MvpDemoActivity"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    <activity
        android:name=".mvvm.view.MvvmDemoActivity"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        
    <!-- UI组件演示 -->
    <activity
        android:name=".ui.CustomViewDemoActivity"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    <!-- Perfetto Trace 演示 -->
    <activity android:name=".performance.PerfettoDemoActivity"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:exported="true" />

  </application>

</manifest>